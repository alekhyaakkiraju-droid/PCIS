apiVersion: v2
name: pcis-platform
description: >
  PCIS platform bootstrap for dev/test — PostgreSQL, Redis, Keycloak, and optional Kafka.
  Deploy once per namespace before microservices (sync-wave 0 in Argo CD).
type: application
version: 0.1.0
appVersion: "1.0.0"
keywords:
  - pcis
  - bootstrap
  - postgresql
  - keycloak
maintainers:
  - name: PCIS Team
