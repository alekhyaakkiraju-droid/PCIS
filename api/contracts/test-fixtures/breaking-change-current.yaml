openapi: 3.1.0
info:
  title: Fixture Current Breaking
  version: 1.0.0
paths:
  /v1/example:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                required: [id]
                properties:
                  id:
                    type: string
