CREATE TABLE IF NOT EXISTS batch_demo_t (
    ID BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    VALUE_TEXT VARCHAR(50) NOT NULL
);

TRUNCATE batch_demo_t RESTART IDENTITY CASCADE;
INSERT INTO batch_demo_t (VALUE_TEXT) VALUES ('one'), ('two'), ('three');
