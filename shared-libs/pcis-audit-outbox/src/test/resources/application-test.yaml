spring:
  application:
    name: pcis-audit-outbox-test
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

logging:
  level:
    com.pcis.audit.outbox: DEBUG
