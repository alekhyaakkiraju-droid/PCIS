apiVersion: batch/v1
kind: CronJob
metadata:
  name: delinquency-aging-batch
  labels:
    app: billing-svc
    job: delinquencyAgingJob
spec:
  # Placeholder — nightly schedule assumption pending Phase 0 baseline measurement
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 3600
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: billing-svc
              image: billing-svc:latest
              args: ["--spring.batch.job.name=delinquencyAgingJob"]
              envFrom:
                - configMapRef:
                    name: billing-svc-config
              env:
                - name: DATASOURCE_URL
                  valueFrom:
                    secretKeyRef:
                      name: billing-svc-db
                      key: url
                - name: DATASOURCE_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: billing-svc-db
                      key: username
                - name: DATASOURCE_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: billing-svc-db
                      key: password
              resources:
                requests:
                  cpu: 250m
                  memory: 512Mi
                limits:
                  cpu: "1"
                  memory: 1Gi
