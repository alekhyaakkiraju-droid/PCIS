package com.pcis.authz.domain;

/**
 * Marker for the authorization domain layer. Business rules and value objects will live here;
 * framework types must not be imported into this package (enforced by ArchUnit).
 */
public final class DomainLayer {

  private DomainLayer() {}
}
