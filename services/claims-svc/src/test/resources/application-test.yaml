spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost/test-issuer
  flyway:
    locations: classpath:db/migration/claims

logging:
  level:
    root: INFO
    com.pcis.claims: DEBUG
    org.flywaydb: INFO
