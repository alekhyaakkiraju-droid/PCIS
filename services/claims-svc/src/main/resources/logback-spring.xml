<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- PII masking converter: SSN, phone, email, tax ID redaction before log persistence -->
  <conversionRule conversionWord="piiMask"
                  converterClass="com.pcis.claims.logging.PiiMaskingConverter"/>

  <!-- Production / default: structured JSON via logstash-logback-encoder -->
  <springProfile name="!test">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <customFields>{"service":"claims-svc"}</customFields>
        <includeMdcKeyName>correlationId</includeMdcKeyName>
        <includeMdcKeyName>actor</includeMdcKeyName>
        <includeMdcKeyName>resource</includeMdcKeyName>
        <includeMdcKeyName>operation</includeMdcKeyName>
        <messageJsonProvider class="net.logstash.logback.composite.loggingevent.MessageJsonProvider"/>
      </encoder>
    </appender>
    <appender name="ASYNC_JSON" class="ch.qos.logback.classic.AsyncAppender">
      <appender-ref ref="JSON"/>
      <queueSize>512</queueSize>
      <discardingThreshold>0</discardingThreshold>
      <includeCallerData>false</includeCallerData>
    </appender>
    <root level="INFO">
      <appender-ref ref="ASYNC_JSON"/>
    </root>
  </springProfile>

  <!-- Test profile: plain-text with PII masking for readable test output -->
  <springProfile name="test">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %piiMask%n</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
    <logger name="com.pcis.claims" level="DEBUG"/>
  </springProfile>

</configuration>
