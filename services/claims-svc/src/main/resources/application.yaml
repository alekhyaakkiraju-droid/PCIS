server:
  port: 8086

spring:
  application:
    name: claims-svc
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/pcis_claims}
    username: ${DATASOURCE_USERNAME:pcis_claims_app}
    password: ${DATASOURCE_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      validation-timeout: 5000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration/claims
    baseline-on-migrate: false
    validate-migration-naming: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${OIDC_ISSUER_URI:https://idp.example.com/realms/pcis}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      properties:
        enable.idempotence: true

pcis:
  outbox:
    relay-enabled: ${PCIS_OUTBOX_RELAY_ENABLED:true}
    relay-interval-ms: ${PCIS_OUTBOX_POLL_INTERVAL_MS:5000}
    relay-max-retries: 3
    kafka-topic: ${PCIS_CLAIMS_EVENTS_TOPIC:claims-events}
    dlq-kafka-topic: ${PCIS_CLAIMS_EVENTS_DLQ_TOPIC:claims-events-dlq}
    metrics-namespace: claims_outbox

management:
  endpoints:
    web:
      exposure:
        include: health,readiness,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized
      group:
        liveness:
          include: livenessstate
        readiness:
          include: readinessstate,db
        startup:
          include: readinessstate,db
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

logging:
  level:
    root: INFO
    com.pcis.claims: INFO
