name: "CI/CD - pcis authz-svc (dev)"

on:
  push:
    branches: [main]
    paths:
      - services/authz-svc/**
      - helm/charts/authz-svc/**
      - .github/workflows/cicd-pcis-authz-svc-dev.yaml
      - shared-libs/**
      - config/**
      - pom.xml
      - docker/**
      - helm/values-dev-shared.yaml
      - .github/workflows/shared-deploy.yaml
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: false
        default: "manual deploy"

permissions:
  contents: write
  security-events: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/shared-deploy.yaml
    secrets: inherit
    with:
      service: "authz-svc"
      port: "8083"
      ecr_repo: "opsera/pcis-authz-svc"
      dockerfile: "services/authz-svc/Dockerfile"
