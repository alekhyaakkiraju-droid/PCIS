name: "CI/CD - pcis billing-svc (dev)"

on:
  push:
    branches: [main]
    paths:
      - services/billing-svc/**
      - helm/charts/billing-svc/**
      - .github/workflows/cicd-pcis-billing-svc-dev.yaml
      - shared-libs/**
      - config/**
      - pom.xml
      - docker/**
      - helm/values-dev-shared.yaml
      - .github/workflows/shared-deploy.yaml
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: false
        default: "manual deploy"

permissions:
  contents: write
  security-events: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/shared-deploy.yaml
    secrets: inherit
    with:
      service: "billing-svc"
      port: "8085"
      ecr_repo: "opsera/pcis-billing-svc"
      dockerfile: "services/billing-svc/Dockerfile"
