name: "CI/CD - pcis claims-svc (dev)"

on:
  push:
    branches: [main]
    paths:
      - services/claims-svc/**
      - helm/charts/claims-svc/**
      - .github/workflows/cicd-pcis-claims-svc-dev.yaml
      - shared-libs/**
      - config/**
      - pom.xml
      - docker/**
      - helm/values-dev-shared.yaml
      - .github/workflows/shared-deploy.yaml
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: false
        default: "manual deploy"

permissions:
  contents: write
  security-events: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/shared-deploy.yaml
    secrets: inherit
    with:
      service: "claims-svc"
      port: "8086"
      ecr_repo: "opsera/pcis-claims-svc"
      dockerfile: "services/claims-svc/Dockerfile"
