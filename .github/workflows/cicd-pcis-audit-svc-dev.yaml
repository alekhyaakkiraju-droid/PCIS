name: "CI/CD - pcis audit-svc (dev)"

on:
  push:
    branches: [main]
    paths:
      - services/audit-svc/**
      - helm/charts/audit-svc/**
      - .github/workflows/cicd-pcis-audit-svc-dev.yaml
      - shared-libs/**
      - config/**
      - pom.xml
      - docker/**
      - helm/values-dev-shared.yaml
      - .github/workflows/shared-deploy.yaml
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: false
        default: "manual deploy"

permissions:
  contents: write
  security-events: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/shared-deploy.yaml
    secrets: inherit
    with:
      service: "audit-svc"
      port: "8080"
      ecr_repo: "opsera/pcis-audit-svc"
      dockerfile: "services/audit-svc/Dockerfile"
